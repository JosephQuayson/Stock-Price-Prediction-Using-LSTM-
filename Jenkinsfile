pipeline{
    agent any 
    stages{
         stage('clone'){
            steps {
                sh 'echo clone1'
                sh 'echo clone2'
            }
         }
          stage('build'){
            steps {
                sh 'echo build'
            }
         }
    }
}
